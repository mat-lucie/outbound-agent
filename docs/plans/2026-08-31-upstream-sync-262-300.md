# Upstream sync 3 — port lucie-sales-agent #262→#300 (anchor `d70e149` → `319f733`)

Third engine sync from the private upstream (`LucieOps/lucie-sales-agent`, branch `dev`,
local checkout `/Users/matiaslt/ClaudeCoding/Lucie/lucie-sales-agent`) into this public
fork. Fork baseline: `main` @ `8ff1a03` (all 13 stacked PRs of the 2026-07-20 sync merged;
upstream state #260 + the #261 email-compliance backport, which ORIGINATED here).

Net upstream delta: 51 commits, ~29.5k insertions / 124 files.

## Method (same as the 2026-07-20 sync)

- Themed branches, stacked in order (each based on the previous), one PR each,
  squash-merged bottom-up once `quality-gates` CI is green.
- Port the upstream diff, then apply the standing adaptation seams:
  - `LUCIE_*` env → `OUTBOUND_*` (with `LUCIE_*` back-compat alias), `~/.lucie-sales` → `~/.outbound-agent`
  - CRM access via `CRMProvider` or the sanctioned `_attio_inner_client(crm)` escape hatch (tracked in CONTRACT.md)
  - new thresholds/IDs → `clients/outreach_config.py` / `clients/pb_config.py` / `config/icp.example.yaml` knobs, mirrored in `examples/acme/config/`
  - operator copy/keywords → neutral placeholder in `content/` + synthetic acme values in `examples/acme/`
  - NO real prospect/company/person names, no live IDs: `scripts/check_no_secrets.py` (incl. `NAMES_DENYLIST`) must stay green
- Gates per branch: `ruff check`, `mypy` (no NEW errors), secrets gate, `python -m pytest -q` (no fail).
- Behavioral branches get an adversarial review (port-fidelity + silent-failure lenses) before PR.
- Final whole-stack review over `main...<top branch>` before declaring done.

## Branches (stack order) → upstream commits

1. `sync3/attio-reliability` — #262 test revive, #263 AttioWriter retry 500s, #264 finalize
   queue-read retries, #266 None→`[]` unsets, #273 `last_migrated_by` provenance stamping
   (engine + schema manifest; skip the dated migration scripts).
2. `sync3/misc-hardening` — #270 phase timers (metrics.py), #275 parallel bulk company
   prefetch, #271 HQ-country backfill + lane-default advisory, #272 person-level language
   override, #267 content lint enforcement (the lint/test only — copy edits are Lucie data;
   apply the rule to neutral + acme content), generic scripts `backfill_company_hq_country.py`
   / `set_person_language.py` if operator-neutralizable.
3. `sync3/experiments-finalize` — #246 per-step attribution (`dm{N}_sent_at` stamps +
   insufficient_data verdict guard), #248 finalize net-new vs re-stamped visibility (skill-doc
   half skipped), #281 experiment-registry preflight shared helper (#283's pain-signal wiring
   deferred to branch 8).
4. `sync3/dedup-identity` — #276 #277 #278 LinkedIn profile-id identity key + slug-variant
   leak closure across all dedup surfaces + READ-side PB CSV match-back bridge.
5. `sync3/sender-protocol` — NET of #279 (Sender seam, PBSender, BotdogSender, botdog client,
   ingest/ledger/limits, send_channel routing) + trial fixups (3d854a0, 422a047, ab5efb6,
   5c0ffc7) + #286 `{leadId,text}` DTO + #287 blank-copy batch halt + #288 review-gap guards
   + #295 blacklist entries endpoint + #296 retire-Botdog default (PB-owned sending).
   Port the FINAL state, not the churn. Campaign IDs/account IDs → config knobs with acme
   placeholders; the Lucie never-contact seeding specifics → neutral config-driven denylist.
6. `sync3/email-gate` — 7502ac0 drip-lane disarm behind `OUTBOUND_EMAIL_ENABLED`
   (`email_lane_gate.py`), #249 radar-wide email hard-decline suppression +
   `email_responded` annotation (`cross_channel_suppression.py` delta).
7. `sync3/followup-radar-gmail` — #297 family (mail.<brand> silencing, ESP-domain bulk-sender
   detection, vendor-domain humans), #299 ball-direction fix (per-counterparty union grouping,
   auto-responses don't set direction), `scripts/gmail_sweep.py` (new, operator-neutral).
8. `sync3/pain-signal` — #280 discovery lane, #282 posters-only mode, #284 engager reshape +
   daily Phase 0.9, #285 direct workers/topic gate/client-side recency, #283 preflight wiring.
   `content/pain_keywords.json` → neutral placeholder + acme example. OFF BY DEFAULT.
9. `sync3/qualifier-integrator` — #298 `disqualifier_integrator` conjunction family
   (description keyword AND industry_vertical=="Other" non-unknown; carve-outs subject-carrying).
   Keyword lists → `config/icp.example.yaml` section + acme mirror, per the #189/#222 pattern.
   Skip `scripts/audit_integrator_prospects.py` unless trivially neutralizable.
10. `sync3/weekly-cursor` — #300 stable per-search csvName + `workflows/scrape_cursor.py`
    ingest cursor (stops PB result recycling).

## Deliberately NOT ported (Lucie operator data / docs / dated one-offs)

- #265, #268 (sales-daily SKILL.md ops docs), docs-only commits `fd553eb`, `0cca424`, `5963f8b`.
- #269 DM2 copy rewrite (Lucie outreach content; fork ships neutral + acme content).
- #261 (`6c08c71`) — already present; it was backported FROM this fork. Verify no drift only.
- Dated one-off scripts: `reconcile_stale_connection_sent_degree_20260805.py`,
  `migrate_attio_people_last_migrated_by.py`, `migrate_attio_send_channel.py` (schema deltas
  go through `scripts/setup_attio_schema.py` idempotently instead).
- Upstream SKILL.md operational-skill deltas (fork keeps its own skills).

## Status

- [ ] Branch 1 attio-reliability
- [x] Branch 2 misc-hardening (committed locally; #270 #275 #271 #272 #267-lint, plus generic scripts backfill_company_hq_country + set_person_language)
- [x] Branch 3 experiments-finalize (committed locally; #246 per-step attribution salvage,
      #248 finalize net-new-vs-restamped visibility — the US-ramp half skipped, no `us_mode`
      invite lane exists in fork engine code, #281 `_experiment_registry_preflight` shared
      helper; #283's pain-signal wiring still deferred to branch 8)
- [ ] Branch 4 dedup-identity
- [ ] Branch 5 sender-protocol
- [ ] Branch 6 email-gate
- [ ] Branch 7 followup-radar-gmail
- [ ] Branch 8 pain-signal
- [ ] Branch 9 qualifier-integrator
- [ ] Branch 10 weekly-cursor
- [ ] Whole-stack review, PRs merged bottom-up
